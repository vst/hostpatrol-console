{
  "name": "hostpatrol-console",
  "description": "Host Patrol Console Application",
  "private": true,
  "scripts": {
    "dev": "vite",
    "format:prettier": "prettier --write .",
    "format": "pnpm run format:prettier",
    "lint:format:prettier": "prettier --check .",
    "lint:format": "pnpm run lint:format:prettier",
    "lint": "pnpm run lint:format",
    "check": "pnpm run lint && pnpm run build",
    "clean": "rm -rf dist",
    "preview": "vite preview",
    "build": "vite build"
  },
  "dependencies": {
    "xmlui": "0.11.24"
  },
  "devDependencies": {
    "@prettier/plugin-xml": "^3.4.2",
    "prettier": "^3.9.6",
    "typescript": "^7.0.2",
    "vite": "^7.3.6",
    "vite-plugin-static-copy": "^3.4.0"
  },
  "prettier": {
    "printWidth": 80,
    "proseWrap": "always",
    "useTabs": false,
    "plugins": [
      "@prettier/plugin-xml"
    ],
    "overrides": [
      {
        "files": [
          "*.js",
          "*.ts"
        ],
        "options": {
          "singleQuote": false
        }
      },
      {
        "files": "*.xmlui",
        "options": {
          "parser": "xml",
          "xmlWhitespaceSensitivity": "ignore",
          "xmlQuoteAttributes": "double"
        }
      }
    ]
  }
}
